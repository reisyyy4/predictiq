name: predictiq
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0
  google_fonts: ^6.1.0
  intl: ^0.19.0
  shared_preferences: ^2.2.3
  qr_flutter: ^4.1.0
  http: ^1.2.0
  mobile_scanner: ^3.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # Tambahkan baris ini untuk register assets
  assets:
    - assets/logo1.png
